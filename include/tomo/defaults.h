#pragma once

namespace tomo {
  namespace defaults {
    typedef double CoordType;
    typedef unsigned long TimeType;
  }
  typedef defaults::CoordType DefCoordType;
  typedef defaults::TimeType  DefTimeType;
}}
